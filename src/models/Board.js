class Board {
  constructor(bid, bname, bcontent, btitle, bdate, bhit, bstep, bgroup, bindent) {
    this.bid = bid
    this.bcontent = bcontent
    this.bname = bname
    this.btitle = btitle
    this.bdate = bdate
    this.bhit = bhit
    this.bstep = bstep
    this.bgroup = bgroup
    this.bindent = bindent
  }
}
